{
  "_comment": "DEVOPS INSIGHTS DASHBOARD - BACKEND PACKAGE CONFIGURATION. This package.json defines all dependencies, scripts, and metadata for the Node.js backend server that powers the DevOps insights dashboard. It uses Express.js for minimal HTTP endpoints (health checks, metrics, status only) with WebSocket (ws) as the primary communication method for all main application functionality, Redis for session management and pub/sub messaging, and TypeScript for type safety. Express handles only essential system monitoring endpoints while WebSockets handle all dashboard data, authentication, and user interactions.",
  "name": "server",
  "version": "1.0.0",
  "description": "This is the backend of the devops insights dashboard",
  "main": "dist/src/app.js",
  "scripts": {
    "scripts": {
      "start": "cross-env NODE_ENV=production pm2 start dist/src/app.js -i max --no-autorestart && pm2 logs",
      "stop": "pm2 stop all",
      "test": "jest --coverage -w=1 --runInBand --detectOpenHandles"
    },
    "test": "jest --coverage=true -w=1 --forceExit --detectOpenHandles --watchAll=false",
    "dev": "nodemon --exec ts-node -r tsconfig-paths/register src/app.ts | ./node_modules/.bin/bunyan",
    "lint:check": "eslint 'src/**/*.ts'",
    "lint:fix": "eslint --fix 'src/**/*.ts'",
    "prettier:check": "prettier --check 'src/**/*.{ts,json,md}'",
    "prettier:fix": "prettier --write 'src/**/*.{ts,json,md}'",
    "build": "ttsc -p ."
  },
  "author": "Kasoma Ibrahim",
  "license": "ISC",
  "devDependencies": {
    "@types/bunyan": "^1.8.11",
    "@types/compression": "^1.8.1",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.2.0",
    "@types/swagger-stats": "^0.95.11",
    "@types/ws": "^8.18.1",
    "eslint": "^9.32.0",
    "jest": "^30.0.5",
    "nodemon": "^3.1.10",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "ttypescript": "^1.5.15",
    "typescript": "^5.9.2",
    "typescript-transform-paths": "^3.5.5",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "@socket.io/redis-adapter": "^8.3.0",
    "axios": "^1.11.0",
    "bunyan": "^1.8.15",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "http-status-codes": "^2.3.0",
    "mongoose": "^8.17.0",
    "redis": "^5.8.0",
    "socket.io": "^4.8.1",
    "swagger-stats": "^0.99.7",
    "ws": "^8.18.3",
    "pm2": "^5.4.0",
    "cross-env": "^7.0.3"
  }
}
