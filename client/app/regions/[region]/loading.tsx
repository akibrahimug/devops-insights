"use client";

/** Page Loading: region detail skeleton. */

import { RegionDetailSkeleton } from "@/components/dashboard/Skeletons";

export default function Loading() {
  return <RegionDetailSkeleton />;
}
